name: Deploy NEXUS HUB

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Compute image name
        id: image
        shell: bash
        run: |
          IMAGE_NAME="ghcr.io/${GITHUB_REPOSITORY,,}:latest"
          echo "name=${IMAGE_NAME}" >> "$GITHUB_OUTPUT"

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push backend image
        uses: docker/build-push-action@v6
        with:
          context: ./NEXUSHUB
          file: ./NEXUSHUB/Dockerfile
          push: true
          tags: ${{ steps.image.outputs.name }}

      - name: Configure SSH
        shell: bash
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          printf '%s\n' "${{ secrets.SSH_DEPLOY_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H "${{ vars.DEPLOY_HOST }}" >> ~/.ssh/known_hosts

      - name: Upload compose file
        shell: bash
        run: |
          ssh -i ~/.ssh/deploy_key "${{ secrets.SSH_USERNAME }}@${{ vars.DEPLOY_HOST }}" "mkdir -p ~/nexushub"
          scp -i ~/.ssh/deploy_key ./NEXUSHUB/docker-compose.yml "${{ secrets.SSH_USERNAME }}@${{ vars.DEPLOY_HOST }}:~/nexushub/docker-compose.yml"

      - name: Deploy on server
        shell: bash
        run: |
          ssh -i ~/.ssh/deploy_key "${{ secrets.SSH_USERNAME }}@${{ vars.DEPLOY_HOST }}" <<'EOF'
          set -eu
          cd ~/nexushub
          cat > .env <<ENV
          IMAGE_NAME=${{ steps.image.outputs.name }}
          APP_PORT=${{ vars.APP_PORT }}
          SPRING_DATASOURCE_URL=${{ vars.SPRING_DATASOURCE_URL }}
          SPRING_DATASOURCE_USERNAME=${{ vars.SPRING_DATASOURCE_USERNAME }}
          SPRING_DATASOURCE_PASSWORD=${{ secrets.DB_PASSWORD }}
          ENV
          docker compose pull
          docker compose up -d
          EOF
