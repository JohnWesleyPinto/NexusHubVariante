spring:
  application:
    name: nexus-hub
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/nexushub}
    username: ${SPRING_DATASOURCE_USERNAME:nexushub}
    password: ${SPRING_DATASOURCE_PASSWORD:nexushub}
    driver-class-name: ${SPRING_DATASOURCE_DRIVER_CLASS_NAME:org.postgresql.Driver}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: ${SPRING_JPA_DATABASE_PLATFORM:org.hibernate.dialect.PostgreSQLDialect}
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: ${SPRING_FLYWAY_BASELINE_ON_MIGRATE:false}
    baseline-version: ${SPRING_FLYWAY_BASELINE_VERSION:1}

server:
  port: 8080
  servlet:
    session:
      cookie:
        http-only: true
        same-site: lax
        secure: ${SESSION_COOKIE_SECURE:false}
      timeout: 30m

app:
  payment:
    fake: ${APP_PAYMENT_FAKE:true}
    access-token: ${MERCADO_PAGO_ACCESS_TOKEN:}
    webhook-secret: ${MERCADO_PAGO_WEBHOOK_SECRET:}
    base-url: ${MERCADO_PAGO_BASE_URL:https://api.mercadopago.com}
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://localhost:4200,http://localhost:8080,http://localhost:8085,http://dsc.rodrigor.com,https://dsc.rodrigor.com,http://eq01.dsc.rodrigor.com,https://eq01.dsc.rodrigor.com}
  google:
    client-id: ${GOOGLE_CLIENT_ID:412019413615-ee1rcn0tq3vc14e6fs8vurjirdr1i69t.apps.googleusercontent.com}
  ai:
    base-url: ${OPENAI_BASE_URL:https://llm.rodrigor.com/v1}
    api-key: ${OPENAI_API_KEY:}
    model: ${OPENAI_MODEL:gpt-4o-mini}

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    root: INFO
    org.springframework: INFO
    br.ufpb.dsc.nexushub: INFO
  file:
    name: logs/nexushub.log
  logback:
    rollingpolicy:
      max-file-size: 10MB
      max-history: 7
